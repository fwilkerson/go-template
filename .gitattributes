# Generated files: collapsed in GitHub diffs and excluded from language stats.
*_templ.go linguist-generated=true
**/static/app.css linguist-generated=true

# Vendored assets: not the project's code.
**/static/*.min.js linguist-vendored=true

# A project's README replaces this one; keep it when merging the template.
# Needs `git config --global merge.ours.driver true` (see README).
README.md merge=ours
